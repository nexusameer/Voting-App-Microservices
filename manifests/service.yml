apiVersion: v1
kind: Service
metadata:
    name: nexusameervotingappmicroservices
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: nexusameervotingappmicroservices